name: One-shot final Phases 7-12 review

on:
  push:
    branches:
      - agent/free-business-phases-7-12

permissions:
  contents: write

jobs:
  review:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/free-business-phases-7-12
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
      - name: Enable package manager
        run: |
          corepack enable
          corepack prepare pnpm@11.4.0 --activate
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Apply final review corrections
        run: python scripts/apply-final-phase-review.py
      - name: Format repository changes
        run: pnpm format
      - name: Remove one-shot helpers
        run: rm scripts/apply-final-phase-review.py .github/workflows/one-shot-final-phase-review.yml
      - name: Commit final review corrections
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Complete lifecycle recovery and upload safety review"
          git push origin HEAD:agent/free-business-phases-7-12
